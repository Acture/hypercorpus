[project]
name = "webwalker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "acture", email = "acturea@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
webwalker = "webwalker:main"

[build-system]
requires = ["uv>=0.5.26,<0.6"]
build-backend = "uv"
